# HoBPI — «Банды Петербурга»

**Мини-апп:** https://photoelf.github.io/hobpi/ · **API:** Cloudflare Worker `hobpi-api`

Пошаговая тактика в духе **Heroes of Might & Magic III**, перенесённая в
криминально-сатирический Петербург 90-х, с асинхронным PvP между Telegram-аккаунтами
в духе старого «Бойцовского клуба». Мобайл-фёрст Telegram Mini App.

> Полное описание — в [`docs/`](docs/). Начать стоит с [00-vision.md](docs/00-vision.md)
> и [09-mvp-scope.md](docs/09-mvp-scope.md).

## Что уже работает (MVP v0.1)

- **Боевой движок**: сетка 12×9, стеки, инициатива с правилом ожидания HoMM3,
  формула урона HoMM3, ответные удары, мораль и удача, 17 свойств бойцов, 9 приёмов героя.
  Детерминированный, изоморфный (сервер + клиент), реплей из `seed + actions`.
- **Контент**: 4 фракции × 7 тиров = 28 бойцов, 3 класса героя, 18 артефактов в 8 слотах,
  13 зданий, 6 типов точек дохода, 6 недельных событий.
- **Экономика**: 4 ресурса, ленивое начисление дохода/прироста/топлива, найм, постройки, прокачка.
- **PvE**: нейтральные «дворы» под силу игрока, интерактивный бой, дроп артефактов.
- **PvP**: асинхронные разборки (ручные и автобой), матчмейкинг, Elo, реальные потери, реплеи.
- **Клиент**: онбординг, 5 экранов + герой, экран боя, проигрыватель реплеев, тема Telegram.

Баланс фракций измерен и держится в коридоре 40–60% при выраженных контр-пиках
(см. [01-gdd.md §9](docs/01-gdd.md)).

## Быстрый старт

```bash
npm install
npm run dev          # api :8080 + web :5173
```

Открой http://localhost:5173 — вне Telegram включается dev-авторизация.
Чтобы играть за нескольких «игроков» одновременно, добавь `?dev=2`, `?dev=3` и т.д.
(id запоминается в localStorage).

Только сервер или только клиент:

```bash
npm run dev:api
npm run dev:web
```

## Команды

| Команда | Что делает |
|---|---|
| `npm run verify` | Всё сразу: типы + тесты + сквозная проверка интерфейса |
| `npm test` | Тесты: движок (16) + сквозной API (19) |
| `npm run test:engine` | Тесты боевого движка и контента |
| `npm run test:api` | Сквозной тест игрового цикла на временной БД |
| `npm run test:ui` | Сборка + прогон мини-аппа в jsdom против живого API (20 проверок) |
| `npm run typecheck` | Проверка типов во всех трёх пакетах |
| `npm run build` | Продакшен-сборка мини-аппа |
| `npm run balance -- 400` | Отчёт по балансу фракций (винрейты + матрица матчапов) |
| `npm run autotune -- 20 120` | Автоподбор масштаба урона линеек под винрейт 50% |

## Структура

```
docs/               проектная документация (видение, GDD, механики, API, роадмап, промпты арта)
packages/engine/    @hobpi/engine — боевой движок и весь контент (TS, без зависимостей)
apps/api/           Fastify + node:sqlite, авторитетный сервер
apps/web/           React + Vite, Telegram Mini App
tools/              balance.ts, autotune.ts — балансировка; ui-smoke.ts — прогон интерфейса
```

## Прод

Telegram требует HTTPS. Есть две схемы — подробности в
[07-architecture.md §7](docs/07-architecture.md).

### GitHub Pages (фронт) + Cloudflare Worker (API) — текущая схема

Pages отдаёт только статику, поэтому там живёт **только мини-апп**. API нужен отдельно:
без него нет ни PvP между аккаунтами, ни рейтинга, ни сохранений, ни защиты от накрутки.

Фронт публикуется сам при пуше в `main` (`.github/workflows/deploy-web.yml`),
адрес API берётся из переменной репозитория `API_URL`.

Первый деплой API:

```bash
npx wrangler login                       # интерактивно, один раз
cd apps/api
npx wrangler d1 create hobpi             # id из вывода вписать в wrangler.toml
npx wrangler d1 execute hobpi --file=schema.sql --remote
npx wrangler secret put BOT_TOKEN        # токен бота от BotFather
npx wrangler deploy
```

Дальше обновления — просто `npm run deploy -w @hobpi/api`.

Почему Workers, а не Render: на бесплатном тарифе Render контейнер засыпает
после 15 минут простоя, и первый заход в мини-апп упирается в 30–50 секунд
холодного старта. Workers не спят, а D1 — тот же SQLite, что и локально.

### Один процесс (альтернатива, нужна VPS)

```bash
npm run build
cd apps/api && BOT_TOKEN=... DB_PATH=/var/lib/hobpi/game.db node src/server.ts
```

При наличии `apps/web/dist` сервер раздаёт мини-апп сам. Переменные — в [.env.example](.env.example).

> ⚠️ Без `BOT_TOKEN` сервер поднимается в dev-режиме и пускает любого по заголовку
> `X-Dev-User`. Для публичного запуска токен обязателен.

## Что дальше

Ближайшие шаги и открытые вопросы, требующие решения владельца продукта:
[10-roadmap.md](docs/10-roadmap.md), [11-open-questions.md](docs/11-open-questions.md).

## Требования

Node.js ≥ 22.5 (используется встроенный `node:sqlite` и нативное исполнение TypeScript).
Разрабатывалось и проверялось на Node 24.
